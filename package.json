{
  "name": "xbs-pwa",
  "version": "1.0.0",
  "private": true,
  "description": "Unofficial read-only PWA client for xBrowserSync",
  "license": "MIT",
  "//": "type: module so Node parses public/js/*.js as ES modules, exactly as the browser does",
  "type": "module",
  "scripts": {
    "icons": "node tools/generate-icons.js",
    "test": "node --test \"tests/*.test.mjs\"",
    "serve": "node tools/serve.js"
  },
  "devDependencies": {
    "lzutf8": "^0.6.3"
  }
}
